# 2026-09-03

- 分析官方 Linux 版 deb `com.tencent.workbuddy_5.4.5_amd64.deb`（腾讯官方 deepin/UOS 商店版）：包名 com.tencent.workbuddy，安装路径 `/opt/apps/com.tencent.workbuddy/files`（deepin 应用规范），Depends 含 `deepin-elf-verify`（商店签名校验）；deb 362MB、未压缩 1568MB（13889 文件），含内置 python3.13 运行时（392MB）。
- 官方包跨平台/跨系统残留（Linux 用不到）：Windows 专属 10.1MB/23 文件（weixinpay/win32-x64 的 QmProtectorLib.dll、libTencentSM.dll、wechatpay-cli.exe(5.9MB)；node-pty winpty make_msvc_package.py；python pip distlib 自带 t32/t64.exe）；macOS 专属 24.6MB/558 文件（@tencent/qimei-node/src/mac/QimeiSDKMac.xcframework macos-arm64_x86_64 framework 含 5.2MB 二进制+Headers+_CodeSignature、node-pty prebuilds darwin-* pty.node）；node-pty 非 linux-x64 多平台 .node 1.6MB。跨平台垃圾合计约 37MB（占 2.4%）。koffi 官方反而只带 linux_x64（干净）。
- 对比：我们的重打包 com.xydw.workbuddy 5.5.1-4（deb 143MB/未压缩 649MB）slim 已清 win32/darwin/msvc/koffi 多平台；官方未做这些清理（多出约 37MB 跨平台垃圾 + 392MB 内置 python，官方特有）。潜在遗漏：我们 slim 规则只匹配 `*darwin*`，会漏清 qimei 的 `src/mac/*.xcframework/*.framework`（建议增强规则删 `*.framework`/`*.xcframework`/`*/src/mac`/`*.dSYM`）。
- 已实施：build.sh stage_slim 补充删 `*.xcframework` 与 `*/src/mac`（qimei 的 macOS bundle）；`.framework`/`*.dSYM`/`*darwin*` 原有规则已覆盖。本地重跑 --skip-extract 验证 build OK，产出 5.5.1-5（143M），已 commit/push(22c5e6b)。当前 5.5.1 来源不含这些 mac bundle，故体积无变化，规则为防御性（来源含 qimei mac xcframework 时生效）。
- 体积差异拆解（官方5.4.5 1568MB vs 我们5.5.1 ~649MB，解包差~919MB）：① runtime 591MB(python3.13 392 + **node 199**)——官方内置、我们不内置(依赖系统python3+electron自带node)；② node_modules **整体差 258MB**(官方283 vs 我们25)含全平台JS依赖+linux原生模块，**纯跨平台可执行垃圾实测仅 29MB**(win32/darwin .node 10 + .dll 2 + .framework/.xcframework 各1)——此前误归为"258MB跨平台.node"；③ cli 25 + resources 12 + locales 40(官方42 vs 我们2) 等零星。app.asar 近似(275 vs 282)。chromium周边差~52MB。
- 官方5.4.5 重打包：安全版 slim(保留runtime,删跨平台垃圾)产 com.tencent.workbuddy_5.4.5-slim_amd64.deb(354MB,解包1541MB,瘦29MB)；激进版 slim2(连runtime删)产 com.tencent.workbuddy_5.4.5-slim2_amd64.deb(266MB,解包954MB,瘦587MB)；slim3(对slim2再精简locale/LICENSES/跨平台残留)产 com.tencent.workbuddy_5.4.5-slim3_amd64.deb(260MB,解包897MB,比slim2瘦56MB)。三者 md5sums 0失败、二进制可起。slim2 风险:官方app或硬编码调 resources/runtime/python,删后依赖python功能可能异常,需桌面实测。
- 更正:此前"对slim2套stage_slim再瘦250MB逼近633MB"是错的——slim2→slim3仅瘦56MB。那252MB node_modules差(官方276 vs 我们24)是官方5.4.5真实依赖(linux原生+全平台JS),非可删垃圾;slim2已无win/mac文件,余276MB为linux必需。我们24MB源于5.5.1 Windows源依赖集更小(换源),非官方包删文件可得。slim3(897)与com.xydw.workbuddy(633)差264MB≈node_modules差,须从Windows源重打包才到633级别。原包deb实测362MB(曾误抄trae的273MB)。
- trae-solo-cn_0.1.61-1 分析（同为拆win重打包，解包1011MB）：electron **39.2.7**(@aha-kit/electron@39.2.7-release.1.51.2) 与 workbuddy 同源；无内置runtime(无 resources/runtime、Depends 不含 python3)；跨平台垃圾已清干净(win32/darwin .node=0, .dll/.dylib/.framework/.dSYM/.xcframework=0)；app 是**解开目录**(resources/app 765MB=modules430+node_modules247+out50+ext23+bin18)非 app.asar；electron 二进制 trae-solo-cn-bin 194MB；私有.so libsscronet 10MB+liblogifier_retrieval 4MB；usr/bin launcher 含 DDE 适配参数(--disable-dev-shm-usage/--title-bar-style=native/禁后台节流)。比 workbuddy 649MB 大 362MB 全在 app 代码量(VS Code系AI编辑器 vs IM客户端)，非打包优劣。
