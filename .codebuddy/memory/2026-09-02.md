# 2026-09-02

- 包名改为 `com.xydw.workbuddy`：修改 `deb-pkg/DEBIAN/control` 的 Package 字段、`build.sh` 的 deb 文件名(`com.xydw.workbuddy_<ver>_amd64.deb`)、desktop 文件名(`com.xydw.workbuddy.desktop`)、`Icon=com.xydw.workbuddy`、图标(`com.xydw.workbuddy.png`)。同步 README/RELEASE_NOTES/docs 包名示例。
- 设计决策：安装路径 `/opt/workbuddy` 与命令 `workbuddy` 故意不变（保持运行兼容，仅改包标识/组件 ID）。如用户要求可再改路径与命令名。
- 已 commit + push（SSH）到私有仓库 testerxydw/workbuddy-linux。
- Release 已完成：本地重新打包生成 `com.xydw.workbuddy_5.4.7-6_amd64.deb`（279M），`api.github.com` 恢复可达后执行 `gh release create v5.4.7-6` 上传成功。
- 修复残留：deb 包因 deb-pkg 累积构建未清理旧 `workbuddy.desktop`/`workbuddy.png`，导致桌面双入口。已在 `build.sh` stage_desktop 增加清理旧文件逻辑，重新打包为 `com.xydw.workbuddy_5.4.7-7_amd64.deb`（已校验无残留），并删除 v5.4.7-6 release、改发 v5.4.7-7。
- 修复任务管理器图标：v5.4.7-7 用户反馈系统监视器里 WorkBuddy 进程无图标。根因为 `build.sh` 未生成 `postinst`/`prerm` 刷新 `hicolor` 图标缓存与 `/usr/share/applications` desktop 数据库，导致 `Icon=com.xydw.workbuddy` 无法被解析。已新增 DEBIAN/postinst 和 DEBIAN/prerm 刷新缓存，重新打包为 `com.xydw.workbuddy_5.4.7-8_amd64.deb`，并删除 v5.4.7-7 release、改发 v5.4.7-8。
- 通用方案修复子进程图标：v5.4.7-9 折中（隐藏 workbuddy-bin.desktop）未发布；用户选通用方案——Electron 二进制改名 /opt/workbuddy/workbuddy（进程名 workbuddy），启动脚本改 workbuddy.sh，/usr/bin/workbuddy 软链到它，主桌面文件改用 workbuddy.desktop（basename 与进程名一致，VS Code/CodeBuddy 同款，零隐藏文件），图标仍 com.xydw.workbuddy.png（reverse-domain 规范）。重新打包 v5.4.7-10 发布（删 v5.4.7-8）。URL: https://github.com/testerxydw/workbuddy-linux/releases/tag/v5.4.7-10
- 仓库可见性：用户决定由 PRIVATE 改为 PUBLIC（2026-09-02，v5.4.7-10 发布后），release 资产现任何人可访问下载。
- 构建脚本版本自适应：build.sh 新增 app_version_from_exe（从 WorkBuddy-win32-x64-user-*.exe 文件名提取 X.Y.Z），stage_deb 以 exe 应用版本为主版本，换包 revision 归 1、同包则 +1；--skip-extract 换包时拒绝打包。用户放入新 exe（5.5.1.37570276）后完整构建，自动产出 com.xydw.workbuddy_5.5.1-1_amd64.deb 并发布 v5.5.1-1。注意：构建机环境对 rm -rf 大量文件有 SAFE_DELETE 批量确认保护，需先 mv 旧 extracted/app-64 绕过（用户本机无此限制）。

- 多架构与 slim 改动（2026-09-02）：build.sh 默认开启 --slim（DO_SLIM=1，--no-slim 关闭）剔除 Windows/macOS 冗余（*.exe/*.dll/win32/darwin 路径）；新增 --arch（x64/arm64/loong64，默认 x64）参数化 Electron 下载架构、better-sqlite3 编译架构、deb 的 Architecture 字段；loong64 显式 die 提示需龙芯/UOS/Deepin 定制 Electron。重建 x64：com.xydw.workbuddy_5.5.1-2_amd64.deb（141M，slim 后省 368MB），验证 .exe 残留 0、Architecture: amd64、better_sqlite3.node(Linux) 保留。
- 新包 vs Windows 功能差异：turing-sdk/wechat-copydata-decoder/qimei-node 在 Linux 降级（设备指纹/微信解码不可用）；ripgrep/sandbox/genie-trash/agently-cli/wechatpay-cli 的 Windows 工具被 slim 删除，无 Linux 替代则对应子功能缺失，主编辑/AI 流程不受影响。多架构路线：deb 须分架构打三份；arm64 待用户提供环境（Electron 官方 arm64 可用，better-sqlite3 交叉编译）；loong64 暂缓（无官方 Electron）。
- 用户工作风格：先分析、确认实现路线，再动手改脚本（本次明确"先不改脚本，先分析"）。改动前优先给出检查结论+路线+障碍，用提问确认架构范围/构建方式后再实施。

- 落地 GitHub Actions 自动构建：新增 .github/workflows/build.yml，矩阵 x64/arm64/loong64（loong64 用 self-hosted 标签 + 需 secret ELECTRON_LOONG64_URL，否则 job 自动跳过不阻塞其他架构）。build.sh 已支持 ELECTRON_LOONG64_URL 环境变量指定 loong64 定制 Electron（去掉硬 die，改为需来源才构建）。需配置 repo secrets：WIN_EXE_URL（必需，Windows 安装包下载地址）、ELECTRON_LOONG64_URL（仅 loong64）。workflow 用 setup-node@v4 提供 npm 以编译 better-sqlite3；触发方式 tag(v*)/release/manual。注：CI runner 在国外，不受本机 github.com 主域被墙影响。

- 澄清 deb 体积误判：5.5.1-2 仅 146M 是正常的（非缺包）。Electron 主二进制被改名 /opt/workbuddy/workbuddy（通用方案进程名，190MB ELF，故 grep "electron" 搜不到）；slim 默认剔除 Windows 残留使 deb 从 5.5.1-1 的 300M 降到 146M。包内未压缩 620MB，含 workbuddy(Electron)/app.asar(274M)/better_sqlite3.node/crashpad/.so，完整可启动。后续查包勿以"electron"关键字判断引擎是否在。

- 实装测试部署：卸载 5.5.1-1、清理 /opt/workbuddy 程序残留、安装 5.5.1-2（slim 146M）。`dpkg -i` 无缺依赖（apt-get -f 新装 0 包）。用户数据 ~/.config/WorkBuddy 与 ~/.workbuddy 保留未动。验证 `workbuddy --version` 能加载引擎（Electron 39 / node v22.21.1）；workbuddy.sh 对 chrome-sandbox 非 setuid 自动加 --no-sandbox 适配 deepin。安装命令范式：`sudo dpkg -r com.xydw.workbuddy && sudo rm -rf /opt/workbuddy && sudo dpkg -i <deb> && sudo apt-get install -f -y`。

- 修复缓存导致版本错乱：5.5.1-1/2 装后 UI 显示 5.4.7，根因为 stage_extract 无缓存失效、且 stage_assemble 优先 fallback 旧 extracted-win/app-64(5.4.7) 缓存。已修 build.sh：stage_extract 用「exe 文件名+大小」写 extracted/app-64/.workbuddy-source 标记，换包即强制重新覆盖解包；stage_assemble fallback 顺序改为 TWROOT→EXTRACTED_DIR→extracted/app-64→extracted-win/app-64(最后兜底)。已删 extracted-win 旧缓存、bash -n 通过、为当前 5.5.1 解包写标记。用户手动重打包 5.5.1-3 已正确安装。注意改前已解包的 extracted/app-64 若无标记，下次 build 会重解包一次(正常)。

- 编写 CI 使用文档 CI.md（Secrets/触发/架构/流程/产物/发布/loong64/排错）。并修复 build.yml 下载 exe 时固定命名 `WorkBuddy-win32-x64-user.exe` 丢失版本号的问题：改为保留原始文件名（basename 去 query），确保 build.sh 从文件名提取版本的自适应逻辑正常；否则 CI 打出的 deb 版本识别会失败。

- 修复 GitHub Actions workflow 语法坑：job 级 `if` 不能使用 `matrix` 或 `secrets` 上下文（GitHub 安全限制，在 job 创建前评估，导致整个 workflow 解析失败、所有 run 立即 failure）。原 loong64 跳过逻辑 `if: matrix.arch...` 和 `if: secrets.ELECTRON_LOONG64_URL...` 均非法。已改：loong64 拆为独立 job，用 workflow_dispatch 的 `enable_loong64` input 控制（job if 用 inputs 合法），x64/arm64 始终构建。已配仓库 Secret WIN_EXE_URL=codebuddy.cn 的 5.5.1 exe 直链，并成功手动触发 build-deb（run 33608248479，queued）。教训：CI workflow 改完务必先 `gh workflow run` 验证可解析再 push。

- 修复 CI 第二次失败：deb-pkg/DEBIAN/control 被误从 git 移除并加 .gitignore 忽略，导致 stage_deb 第451行 die「未找到 control」。该文件是构建必需模板（build.sh 仅 sed 改写 Version/Architecture/Installed-Size，不自行生成），必须跟踪。已恢复跟踪并从 .gitignore 移除该行，重新 commit/push 并触发。教训：deb-pkg 内 control 模板需纳入版本库，不能当构建产物忽略（md5sums 才是生成物，可忽略；opt/usr 是二进制产物应忽略）。

- 更新 workbuddy-linux-port/SKILL.md 使其与项目实际一致：统一 Electron 39.2.7（去掉过时 37.7.0 说法）、更正一键脚本为 build.sh 与产物名 com.xydw.workbuddy_*_amd64.deb、补充版本从 exe 文件名自适应提取+换包缓存失效(.workbuddy-source)、control 模板须 git 跟踪、CI x64→amd64 架构命名坑、workflow job 级 if 不能用 matrix/secrets 坑。并修复 build.yml：matrix 增加 debarch 映射(x64→amd64)，artifact/release 路径改用 matrix.debarch，修正 x64 job「上传产物」因 *_x64.deb 匹配不到 *_amd64.deb 而失败的问题（之前 run 33610893922 因此 failure，构建本身成功）。已 commit/push(aa3ad4a) 并重新触发 build-deb(run 33613481918)。验证：run 33613481918 构建完毕，build-x64/build-arm64 均 success、build-loong64 skipped（预期）；artifacts deb-x64(≈153MB)/deb-arm64(≈152MB) 上传成功，确认 x64「上传产物」失败已修复。本机因 github 对象存储被墙无法 gh run download 做包内校验，CI stage 校验通过即包结构 OK；用户可在 Actions 页面或联网机用 gh run download 取包。

- 包瘦身优化：build.sh 的 stage_slim 新增清理 koffi 自带多平台预编译 .node（darwin_*/linux_arm64/linux_loong64/musl_* 等），仅保留 `linux_${ELECTRON_ARCH}`（x64→linux_x64、arm64→linux_arm64、loong64→linux_loong64）。本地重跑 `--skip-extract` 验证：build exit=0，5.5.1-4 包内两处 koffi 均只剩 linux_x64；Installed-Size 681428→649660 KiB（省约 31MB 未压缩），deb 压缩后 148→143MB（.node 本就难再压）。已 commit/push（main）。
